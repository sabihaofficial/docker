version: "3"

services:

  demo:
    image: demo
    networks:
      - my-network
    ports:
      - 8080:8080

  database:
    image: mysql
    network:
      - my-network
    environment:
      - MYSQL_ROOT_PASSWORD=admin123
      - MYSQL_DATABASE=demo
networks:
  my-network:



